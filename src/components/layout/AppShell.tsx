// TODO: responsive shell — sidebar layout on wide viewports, collapses to a
// drawer/bottom sheet below a breakpoint (tablet/phone in a storage room is
// the primary use case, not desktop). See ARCHITECTURE.md section 4.

export default function AppShell() {
  return null;
}
