// TODO: edit the selected ShelvingUnit's dimensions, shelf count, name,
// rotation. On narrow viewports this collapses into a bottom sheet/drawer
// rather than a fixed sidebar — see ARCHITECTURE.md section 4.

export default function UnitPropertiesPanel() {
  return null;
}
