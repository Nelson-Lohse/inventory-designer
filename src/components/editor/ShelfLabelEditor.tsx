// TODO: add/edit free-text ShelfLabels for a single Shelf within the
// selected unit. v1 only collects text — no category picker (see
// src/types/reserved-v2.ts for why the field exists but is unused here).

export default function ShelfLabelEditor() {
  return null;
}
