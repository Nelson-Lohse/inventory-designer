import type { UnitTypeTemplate } from '../types';

// TODO: decide whether to seed common commercial shelving sizes here (see
// ARCHITECTURE.md open decision #4) or leave the catalog empty and
// user-populated. Left empty until that's decided.
export const unitTypePresets: UnitTypeTemplate[] = [];
